There are 6 GNN kernels. Enter the kernel subdir.
- For FPGA Compilation, enter hw folder and run the bash script `./build.sh`
- For software emulation, enter sw_emu folder and run the bash script `./build_and_run.sh`
